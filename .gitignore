node_modules
dist
.DS_Store
*.local
.vite

# Generated by `npm run extract`.
# The national concession layer is ~44 MB of derived geometry; regenerate it
# from the source Folium HTML rather than committing it. The two case-cluster
# files are small and ARE committed so a fresh clone is immediately usable.
public/data/concessions/
